$("div").on("click", function() {
  $(this).toggleClass("show-description");
});
/* selects div elements --when clicked on listen for event (the click) if that happens we want to toggleClass show description*/
